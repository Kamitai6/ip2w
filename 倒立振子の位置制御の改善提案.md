# 倒立振子の位置制御の改善提案

## 要請
- コマンド積分だけのときの、10cm程度の振幅で揺れ続ける問題を解決したい
- commandモデルの短期精度は良いが、長期的にバイアスが蓄積してドリフトする
- 加速度センサ単体での並進加速度推定は原理的に困難（後述）なので、
  EKFの「観測」として使うのは不適切。別のアプローチを検討する
- UDPでプリント可能（udp_printlnマクロでcore::fmt::Writeベース）
- 確認済みの符号を再び疑うことを禁止
- リーク積分禁止
- モーターの不感域やデッドゾーンなどの考慮はしなくてよい
    atom_max（MOTOR_SPEED_MAX）の値と、apply_deadzoneの挙動
    pub fn apply_deadzone(input_value: f32, input_limit: f32, output_min: f32, output_max: f32) -> f32 {
        // 1. 入力を制限（安全装置）
        let input = input_value.clamp(-input_limit, input_limit);

        // 2. ほぼ0なら、計算誤差が出ないように完全に0にする
        if input.abs() < EPSILON {
            return 0.0;
        }

        // 3. 比率を計算 (0.0 〜 1.0 の範囲になるので、絶対に桁あふれしない)
        let ratio = input.abs() / input_limit;

        // 4. マッピング (線形補間)
        let output_f32 = output_min + (ratio * (output_max - output_min));

        // 5. 符号を復元
        if input > 0.0 {
            output_f32
        } else {
            -output_f32
        }
    }
    pub const MOTOR_SPEED_MAX: i8 = 127;

### 加速度センサの限界
- IMU(BMI270)のaxから並進加速度を抽出するには、重力成分(g×sinθ)と
  接線加速度(α×r)を引く必要がある
- 引く側の成分が1 m/s²オーダーなのに対し、欲しい並進加速度は
  0.1〜0.5 m/s²オーダー。引き算の残差が信号を上回る（SNR < 1）
- a_raw自体が0.1秒間隔で±1 m/s²変動（符号反転47%）し、
  これはセンサノイズではなく補正残差（物理的加速度の分離不良）
- a_targetとa_measの相関は0.49、符号一致率53%（ほぼランダム）
- LPFで解決不可（汚染成分と信号が同じ0.5〜5Hz帯に重なるため）

### commandモデルの特性
- r_accel=1000（センサ無視、commandモデル100%）の方が：
  - 振動振幅が半減
  - ゼロ交差速度が改善（0.41→0.31 m/s）
  - 手で押しても原点に戻る
  - crashしない
- ただし長期バイアス蓄積の補正手段がなくなる問題がある

### EKFの構造的問題
- a_targetは「入力」であり「観測」ではない
- a_measはSNR < 1で「観測」として機能しない
- 現状で唯一の独立した情報源はposition観測（innovation_pos）だが、
  位置から加速度バイアスを推定するのは間接的すぎて応答が遅い
- EKFの枠組み自体がこの問題にフィットしていない可能性がある

### 制御ゲインの制約
- k_vel > 0.1 → 即座に転倒（0.1秒）
- k_pos ↑ → 発振が発散
- 現在のゲインはこれ以上上げられない上限付近
- 制御構造はカスケード方式（pos_ekf → target_angle → SMC）で正しい

### 未解決の負方向バイアス
- 位置meanが-45〜-54mm（r_accelの値によらず共通）
- PITCH_OFFSETのずれまたは物理的非対称（重心、モータ応答差）の可能性

## 情報
- TORQUE_TO_PWM=10000は正しい（15000で発振、20000で倒立不能）
- 機体重量は100g
- 2つのホイールそれぞれにモーター (ホイール半径は30mm、ホイール軸からセンサまでの距離は80mm、ホイール軸から重心までの距離は35mm)
- 制御周期500Hz、プリント周期10Hz、ピッチオフセット（重心バランス点）は-0.125rad
- 加速度センサと出力PWM（コマンド）を使用して、加速度や速度を含め、位置を推定する手法を使っている
- Jerk制限、加速度制限、速度制限、Jerk符号反転検出によって適応EKFにしている
- 加速度センサの重力成分や接線加速度を除去することで、できるだけ並進加速度に近づける
- commandの回転トルク成分を除去し、モーター効率を考慮することで、できるだけ並進トルクに変換し、並進加速度を計算している
- command正 → 前方に進む（実機確認済み）
- 加速度センサX軸：前進で正（実機確認済み）
- a_rawの式 (ax_g + sinf(pitch_sensor)) * 9.81 の符号は正しい（確認済み、変更禁止）
- commandと並進加速度（a_meas）は閉ループ下で逆位相（グラフで確認済み）
- 加速度センサのみ（command予測なし）で倒立は可能だが、bias推定が追いつかずドリフトが止まらない
- 常に動いている系ではZUPT的な静止区間がなく、加速度だけではbiasの観測可能性が不十分
- tau_aが小さすぎるとaccelがゼロに張り付く（0.02→NG、0.1→NG、1.0→改善）
- j_max=50は小さすぎた（300で改善）
- commandは300でクランプしてモーターに出力しているので、EKFに渡す前にもクランプしている

## 現在のパラメータ
let mut pos_ekf = pos_ekf::PositionEkf::new(pos_ekf::PosEkfConfig {
        dt: DT,

        torque_to_pwm: TORQUE_TO_PWM,
        inertia: 0.000363,
        wheel_radius: 0.03,
        mass: 0.1,
        robot_radius: 0.08,
        motor_efficiency: 0.5,

        j_max: 400.0,
        a_max: 50.0,
        v_max: 5.0,

        command_lpf_tau: 0.016,
        sign_lpf_tau: 0.1,

        r_accel: 1000.0,
        constraint_r_scale: 2.0,
        r_pos: 0.01,

        // プロセス（予測）ノイズ
        q_a_min: 10.0,
        q_a_max: 100.0,
        q_v: 1e-3,
        q_x: 1e-4,

        k_pos: 0.01,
        k_vel: 0.1,
        max_output: 0.4,

        ..Default::default()
    });

## 最新のログフォーマット
udp_println!(
                                    "{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.3},{:.4},{:.4},{:.3},{:.3}",
                                    ps.position, ps.velocity,           // 0,1
                                    ps.a_target, ps.a_meas, ps.a_raw,   // 2,3,4
                                    ps.accel,                   // 5,
                                    ps.innovation, ps.sign_agree_lp,     // 7,8
                                    ps.r_eff,                   // 9,
                                    ps.tangential_cmd, ps.tangential_sensor, // 11,12
                                    ps.k_gain_a, ps.k_gain_pos,           // 13,14
                                    ps.command_lp,                       // 16
                                    ps.innovation_pos,//17
                                );

## 最新ログ