[package]
name    = "control"
version = "0.1.0"
edition = "2024"

[dependencies]
libm = {version = "0.2"}
defmt = "1.0"
nalgebra = { version = "0.34", default-features = false, features = ["libm"] }

[features]
